using System;

class SingleChore : Chore
{
    public SingleChore(decimal cost, string expenseName, string expenseType, string recurrent, string date) : base(cost, expenseName, expenseType, recurrent, date)
    {

    }
}